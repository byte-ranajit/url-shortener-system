services:
  mysql:
    image: 'mysql:latest'
    environment:
      - 'MYSQL_DATABASE=url_shorter_db'
      - 'MYSQL_PASSWORD=root'
      - 'MYSQL_ROOT_PASSWORD=root'
      - 'MYSQL_USER=root'
    ports:
      - '3306:3306'
  redis:
    image: 'redis:latest'
    ports:
      - '6379:6379'
