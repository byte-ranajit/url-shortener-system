spring:
  application:
    name: url-shortener-system

  datasource:
    url: jdbc:mysql://localhost:3306/url_shorter_db
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect